import { DistanceStringPipe } from './distance-string.pipe';

describe('DistanceStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceStringPipe();
    expect(pipe).toBeTruthy();
  });
});
